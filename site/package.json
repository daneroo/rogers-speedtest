{
  "name": "site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "gatsby develop -p 8001",
    "build": "gatsby build",
    "deploy:now": "now --prod && now rm --safe --yes rogers-speedtest",
    "testWithAuditLater": "npm run lint && npm audit && npm run unit",
    "test": "npm run lint && npm run unit",
    "lint": "standard",
    "unit": "echo jest when appropriate",
    "coverage": "jest --collectCoverageFrom=lib/**.js --coverage ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gatsby": "^2.24.47",
    "gatsby-theme-document": "^1.0.10",
    "standard": "^14.3.4"
  },
  "dependencies": {
    "@nivo/line": "^0.62.0",
    "@theme-ui/prism": "^0.3.0",
    "gatsby": "^2.23.3",
    "gatsby-theme-document": "1.0.10",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "theme-ui": "^0.3.1"
  }
}
